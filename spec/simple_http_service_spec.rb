RSpec.describe SimpleHttpService do
  it "has a version number" do
    expect(SimpleHttpService::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
